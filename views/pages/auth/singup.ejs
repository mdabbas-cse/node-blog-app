<!doctype html>
<html lang="en">

<head>
  <%- include('../../partials/head.ejs') %>
</head>

<body>

  <%- include('../../partials/navbar.ejs') %>

    <!-- Section: Design Block -->
    <section class="background-radial-gradient overflow-hidden">

      <div class="container px-4 py-5 px-md-5 text-center text-lg-start my-1">
        <div class="row gx-lg-5 align-items-center mb-5">
          <div class="col-lg-6 mb-5 mb-lg-0" style="z-index: 10">
            <h1 class="my-5 display-5 fw-bold ls-tight" style="color: hsl(218, 81%, 95%)">
              The best contain <br />
              <span style="color: hsl(218, 81%, 75%)">for you</span>
            </h1>
            <p class="mb-4 opacity-70" style="color: hsl(218, 81%, 85%)">
              Lorem ipsum dolor, sit amet consectetur adipisicing elit.
              Temporibus, expedita iusto veniam atque, magni tempora mollitia
              dolorum consequatur nulla, neque debitis eos reprehenderit quasi
              ab ipsum nisi dolorem modi. Quos?
            </p>
          </div>

          <div class="col-lg-6 mb-5 mb-lg-0 position-relative">
            <div id="radius-shape-1" class="position-absolute rounded-circle shadow-5-strong"></div>
            <div id="radius-shape-2" class="position-absolute shadow-5-strong"></div>

            <div class="card bg-glass">
              <div class="card-body px-4 py-5 px-md-5">
                <form method="post" action="/auth/singup">
                  <!-- Email input -->
                  <div class="form-outline mb-2">
                    <label class="form-label text-white" for="username">Username</label>
                    <input type="text" id="username" name="username"
                      class="form-control bg-transparent <%= errors.username ? 'is-invalid' : '' %>"
                      value="<%= values.username && values.username %>" />
                    <span class="text-danger">
                      <%= errors.username && errors.username %>
                    </span>
                  </div>
                  <!-- Email input -->
                  <div class="form-outline mb-2">
                    <label class="form-label text-white" for="email">Email address</label>
                    <input type="email" id="email" name="email"
                      class="form-control bg-transparent <%= errors.password ? 'is-invalid' : '' %>"
                      value="<%= values.email && values.email %>" />
                    <span class="text-danger">
                      <%= errors.email && errors.email %>
                    </span>
                  </div>

                  <!-- 2 column grid layout with text inputs for the first and last names -->
                  <div class="row">
                    <div class="col-md-6 mb-2">
                      <div class="form-outline">
                        <label class="form-label text-white" for="password">Password</label>
                        <input name="password" id="password" type="password"
                          class="form-control bg-transparent <%= errors.password ? 'is-invalid' : '' %>"
                          value="<%= values.password && values.password %>" />
                        <span class="text-danger">
                          <%= errors.password && errors.password%>
                        </span>
                      </div>
                    </div>
                    <div class="col-md-6 mb-2">
                      <div class="form-outline">
                        <label class="form-label text-white" for="confirm-password">Confirm Password</label>
                        <input name="confpassword" id="confirm-password" type="password"
                          class="form-control bg-transparent <%= errors.password ? 'is-invalid' : '' %>" />
                        <span class="text-danger">
                          <%= errors.confpassword && errors.confpassword %>
                        </span>
                      </div>
                    </div>
                  </div>

                  <!-- Submit button -->
                  <button type="submit" class="btn btn-block btn-primary">
                    Login
                  </button>

                </form>
                <a href="/auth/login">Already have a acount?</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    <!-- Section: Design Block -->





    <%- include('../../partials/foot.ejs'); %>
</body>

</html>